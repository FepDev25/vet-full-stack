spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:postgresql://localhost:5435/veterinaria_test
    username: vetuser
    password: vetpass
  jpa:
    hibernate:
      # Solo conecta — no valida ni crea el esquema.
      # Permite testear el contexto sin que el schema completo esté aplicado.
      ddl-auto: none
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: cAehb+Y5NkTSyqY7fd6M4Ha9HXnSIKvU99rGyR70TVMynW4Wd1bb1rn2xYyWZxoQIoLow2eC+M0qX3bl1GlOzw==
    expiration-ms: 86400000
    refresh-expiration-ms: 604800000
  cors:
    allowed-origins: http://localhost:5173
